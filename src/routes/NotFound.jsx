import { useRouteError } from "react-router-dom"

function NotFound() {
  return (
    <main>
      <header>
        <h1>Not found</h1>
      </header>

      <div>
        <p>Sorry, nothing here.</p>
      </div>
    </main>
  )
}

export default NotFound